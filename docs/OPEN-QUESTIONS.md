# 待定问题清单（滚动更新）

> 记录尚未决定的事项。每项处理完后移入文末「已决议」区。
> **最后更新**：2026-09-22（Q13 ED 阈值 **0.78 已落地并写死**、UI 音效 7 个已导出；两者从「待执行」移入「已决议」）
>
> ⚠️ **本文件里引用的 `docs/NN` 编号笔记已于 2026-09-23 归档到 `.tmp/docs-archive/`。**
> 那些引用现在指向归档目录；要查原文请去那里。

---

## 待定

### ⏸ Q2 · 角色范围（用户：延后）
- **内容**：苍崎青子 / 久远寺有珠 / 静希草十郎——三人全做，还是先做单人或双人？
- **背景**：本地素材中三人立绘与差分分别成套存在。
  实测图片数：**青子 `aok` 2257 + `s_aok` 1952 = 4209**；草十郎 `sou` 1366 + `s_sou` 1254 = 2620；有珠 `ari` 902 + `s_ari` 775 = 1677
- **我的建议**（供参考）：先做青子单人，架构预留多人
- **状态**：**用户明确延后**

### ⏸ Q4 · 素材分发边界（用户：延后）
- **内容**：素材不随包（用户自备，最安全）vs 随包分发
- **背景**：美术归 TYPE-MOON / 奈须蘑菇 / 小山广和；社区统一做法是代码 MIT + 美术 CC BY-NC-SA 4.0 + 不随包
- **我的建议**（供参考）：不随包 + 提供一键导入脚本
- **状态**：**用户明确延后**

### 🆕 Q12 · 原作构图坐标从哪提取
- **来源**：B4 决议（2026-09-15）——「全盘复刻游戏里的设计」，不做编辑器
- **问题**：原作的**构图数据**（立绘位置 / 缩放 / 图层 / 转场 / 暗带浓度切换时机）与点击语义一样，
  埋在 **`.chs` 编译字节码**里（`HunexCompiledScriptVer1.00`），命令表在加壳的 `woh_data.dll` 中。
- **已排除的路径**：
  - `.ctd` 只有文本，无坐标
  - `woh_data.dll` 加壳，取不出命令词
  - 此前查按键语义时已确认这条链路走不通
- **可能可行的路径**（待评估）：
  1. **数值提取**：字节码里的坐标是**裸数值**，未加密；若能定位操作数布局，可批量导出
  2. **逆向社区工具**：`nike4613/MahoyoHDRepack` 的 `ScriptText/` 目录已能解析脚本结构，可研究它的解析器
  3. **实测反推**：截图量测（对若干典型场景人工量取）
  4. **降级**：不追求逐场景复刻，只复刻**元界面**（書庫/表紙/環境設定/付箋）这类静态布局——这些已全部实测到手
- **状态**：待评估（不阻塞，因为你说了"到时候我说详细指令"）

### ⏸ Q14 · 皮肤开着时**发不出消息**（用户 2026-09-19：**先别改，往后面拖**）
- **现象**：皮肤挂载时界面收不了消息（输入/发送不可用）；必须先用 `Ctrl+Shift+M` 关掉皮肤才发得出去。
  → 这条直接卡住"人眼验收"，也卡住表情切换的实机验证。
- **已定位到的证据**（**没有改代码**，只是读现场）：
  - `/moye-skin/health` 的探针里 `layout.composerH = 0`、`composerOk = false`；
  - 于是 `--myh-column-bottom` 退化成 0 → `.myh-column`（`pointer-events:auto`、`z-index:4`）铺满整屏，
    把 composer 那一条的点击吃掉了；
  - **选择器本身是对的**：`data-composer-seat / -card / -input` 在 DSH 的
    `@deepseek-ai/dsh-client-ui-conversation/lib/client.js` 里确实存在（已 grep 到）；
  - 但 `[data-composer-seat]` 是带 `ref: seatResizeRef` 的**包装层**，很可能 `height = 0`
    → 被 `rectOf()` 的「宽高都要 ≥4px」判据滤掉；而 `pickVisible` 往下试 `-card`/`-input` 也没量到，
    说明**它们当时也没渲染或被隐藏**。
- **下次动手的顺序（照这个查，别猜）**：
  1. 先加**诊断**（不是修）：把三个候选选择器的 `getBoundingClientRect()` 与
     `getComputedStyle(el).display` 打进探针 —— 先分清"没渲染"还是"被皮肤藏了"；
  2. 重点怀疑皮肤自己的这条连坐规则：
     `div:has(> [data-rightbar-col]) ~ *{display:none!important}`（后面所有兄弟一起被藏）；
  3. 修的方向：**composer 量不到时，`.myh-column` 必须让出底部**（宁可正文下方留白），
     并且热区洞②要以实测的 composer 上沿为准 —— 也就是"量不到就别铺满"。
- **状态**：**用户明确延后**（先别改）。

> **★ 2026-09-19 22:14 补记 —— 第 1 步（加诊断）已完成，且台账描述已过时**
>
> 查代码发现：**`guard` / `layout` 两组字段其实早就写好了**（`skin/lib/client.js` 的 `autoProbe` 里，
> `docs/18` §2 也记了）。台账写"先加诊断"这句**不准确** —— 真正缺的只有**逐候选**那一层：
> `measureShell()` 用的是 `pickVisible()`，它**故意丢掉矩形无效的候选**，
> 于是探针里只剩最终数字 `composerH`，**分不清**"元素不在 DOM 里"和"在 DOM 里但矩形是 0"。
>
> 已补（**纯只读，不参与任何判定，未改行为**）：
> - `composerCandidates()`：对 `[data-composer-seat]` / `[data-composer-card]` / `[data-composer-input]`
>   各自上报 `found` / `count` / `rect` / `display` / `visibility` / `opacity` / `hasOffsetParent` / `childCount`；
> - `siblingHideRuleMatches`：直接查台账第 2 步点名怀疑的连坐规则
>   `div:has(> [data-rightbar-col]) ~ *` 到底匹配到几个；
> - `seatPath`：seat 的 class 与父节点，便于看它是被谁包的。
> - 挂在 `autoProbe.guard.composerCandidates`（**内部**，宿主只留存 `body.autoProbe || body`）。
>
> 质量门：`check-client.mjs` **64 → 71 项**（新增 7 条断言，含"必须挂在 autoProbe 内部"的位置断言
> 与"不许塞 DOM 本体"的红线 7 断言）；7 个脚本全绿；已 `install.ps1`
> （BUILD_ID `92136530C9-0919-221435`）。
>
> **怎么读**：开皮肤 → 刷新页面 → 看 `/moye-skin/health` 的 `diag.probes[].autoProbe.guard.composerCandidates`。
> `found:false` = 没渲染；`found:true` 且 `rect` 高 0 = 渲染了没布局；`display:"none"` = 被藏了。
> **第 2 步（改行为）仍未动，等用户发话。**

---

## 待执行（已决议、尚未落到代码）

> 2026-09-22：**Q13 已从中移出**（阈值写死 0.78 + 面板行删除，均已落地并实机验证），
> 见文末「已决议」区。本节现在只剩下面这一组收尾项。

### ⏸ 其它收尾项（2026-09-19 用户：都先别动，等看完画面 —— 画面看完后再定）
- 打包分发验证（`files` 白名单；素材 **263.2 MiB / 380 个文件** —— 2026-09-22 复测，含 80 个设置页切片）
- `docs/15` 过时处标注（§9.1 红线 6、§9.3、§7.2 的槽位表）
- ~~UI 音效~~ → **已于 2026-09-22 完成**：7 个 `.hw` 从 `data00300.hfa` 导出到 `skin/assets/se/`，`SFX` 表已填（见 Q13 同批落地）

---

## 本轮实测发现（2026-09-19 22:13 · 接续会话，红线 #8 机器化）

- **红线 #8（立绘按素材画布整张输出、不切平头顶）→ 已离线判定通过**，详见 `docs/20-立绘几何离线核查.md`：
  136/136 张输出尺寸 == 来源身体层画布；四档视口（1280×720 ~ 2560×1440）头顶稳定落在画面 **24%** 处，
  离视口顶边与约 44~90px 的 DSH 顶栏都有充裕余量。
- **新工具**：`persona_work/tools/verify_sprite_headroom.py`（退出码 0/1，可并入 `skin/tools/` 检查套件 —— **待用户决定要不要并**）。
- ⚠ **新发现（供验收留意，未改代码）**：`place-items:end center` 居中 + 盒子宽随素材画布变化 ⇒
  **同一景内换表情可能有水平微移**（A3 实测盒子 left 跨度 815~1018px，≈203px；竖向不跳）。
  这与 `docs/15` §11 第 5 条原话「切换不跳位」有出入 —— 待用户看一眼能否接受，**别自己改**。
- **皮肤当前 `killed=true`**（用户上次 Ctrl+Shift+M 逃生留下的）⇒ `probes=[]`、`autoProbe` 无数据。
  `19` §4.3 里"看 `/moye-skin/health`"那条**必须先开皮肤**才有东西可看。
- **时间戳口径提醒**：`/moye-skin/health` 的 `clientLastSeen` 带 `Z`，PowerShell 解析后显示为
  **UTC 字样**（比本地少 8 小时），别误判成"三小时前的心跳"。

---

## 本轮实测发现（2026-09-19 20:27 · 供验收参考）

- **磁盘上的部署 = 当前源码**：装好的 `client.js` `BUILD_ID = 5C240A863C-0919-202546`，
  把源码 `'dev-未注入'` 换成它后**逐字节相同**
- **7 个离线脚本全绿**（exit=0）；`/moye-skin/health`：`clientErrors=[]`、
  探针 `domNodeCount=40`、`hasColumn/hasTranscript=true` → "画出来了"仍成立
- ⚠ **验收前必须刷新**：health 最后心跳的 `clientInfo.build` 是 `8FE9DA6526-0919-202422`（20:24:22 那版），
  而 `5C240A863C`（20:25:46）也刚报过 —— **两份构建同时在跑**（实例号 `m6ikix-2pgs` / `omas85-28ln`）
- ⚠ **BGM 验收现在必然失败**：`settings.yaml` 里 `bgm: false`、`sfx: false`，
  而宿主默认是 `true` → **是被关掉的**，过第 6 条前要先打开
- **假警报排除**：探针里 `myh-sprite<--skin/assets/…`、`store.sprite:"ts/sprite/…"`
  **不是路径坏了** —— 是 `client.js` L2719 `slice(-42)` / L2782 `slice(-32)` 的显示截断
- **`docs/16` §8.3 的指路不准**：它写「`docs/15` §11 里皮肤根注册在 `shell.overlay` 已过时」，
  但 `docs/15` 全文**没有 `shell.overlay` 字样**，§11 本身就是验收清单 →
  真正过时的是 **§9.1 红线 6** 与 **§9.3**
- **已完成**：`skin/README.md` §9 验收表 + §2/§8 的「自建容器」描述**已经改好**
  （README mtime 20:25:34 晚于 `docs/16` 的 20:25:05，清单里那条可以划掉）

---

## 已决议
### Q13 · ED 彩蛋阈值 → **0.78 固定值，已落地**（2026-09-22）
- **决议日**：2026-09-19（决议）／**落地日：2026-09-22**
- **决议**：`edThreshold` 固定 **0.78**；设置面板里「触发阈值」那一行**整行删掉** ——
  用户明确：**「这是不可修改项」**
- **动机**：DSH 自动压缩阈值是 **0.8**（`dsh-compaction-basic` 的 `DEFAULT_THRESHOLD_RATIO`）。
  皮肤原阈值 0.9 → 永远到不了；yaml 里曾被改成 0.8 → 与压缩线打平，等于赛跑。0.78 卡在压缩线之下
- **实际改动（四处，均已验证）**：
  1. `skin/lib/client.js` 判断处写死 `0.78`（不再读设置）
  2. `skin/lib/client.js` 面板「触发阈值」三档 → **整行删除**
  3. `skin/lib/client.js` 内置默认 → `0.78`
  4. `skin/lib/index.js` L92 默认值 / L624 schema 默认值 → `0.78`（L119 归一化逻辑保留，把任何越界值夹回 0.78）
- **幽灵值清理**：`~/.dsh/settings.yaml` 里遗留的 `edThreshold: 0.8` 已删除，
  备份在 `recovery/settings.yaml.bak-pre-q13-0921-2137`。**保留 schema 键但强制归一化**，
  这样 yaml 里不会再留一个"看不见但还在"的值
- **测试同步**：`skin/tools/check-client.mjs` 与 `render-selftest.mjs` 的 fixture 已同步为 0.78（套件 216/0 全绿）
- **实机验证（2026-09-22 09:20 重启后）**：`/moye-skin/health` 报 `settings.edThreshold: 0.78`、
  `clientInfo.build: D8B1D9ACCE-0922-085902`（旧值 `49BDE31BE7` / 0.9 均已消失）
- **已知代价（如实记）**：0.78 只比压缩线低 2 个百分点，窗口窄 ——
  只在"单步从 78% 以下跨过 78%、且没越到 80%"时才响。**用户已接受**


### Q1 · 时间带 → 保留概念，但触发改为「系统深色/浅色模式」
- **决议日**：2026-09-15
- **决议**：放弃"跟随真实时钟"，改为跟随 **DSH 主题**：浅色→昼，深色→夜。
- **收益**：消掉了原方案最大风险（凌晨界面变暗干扰干活）；用户切主题是主动行为，预期之内有掌控感；DSH 本来就有 light/dark/system，**零新增状态**
- **扩展**：组织轴可扩展（预留手动指定、按工作区），但**第一版只做深浅色**
- **产出**：`09-待确认项决议.md` B1

### Q3 · 章节感 → 完全不做
- **决议日**：2026-09-15
- **决议**：不做章节推进、不做氛围档位、不做任何"进度"概念
- **定调**：**这是皮肤，不是 GalGame。** 一切"会累积/会推进"的机制都不属于它
- **连带删除**：`02` §2 的「机制 4 章节感」、`05` 的章节感设计
- **感情载体收敛为三条**：时间带（=深浅色）· 状态演出 · 系统事件台词化

### Q5 · 场景可视化编辑器 → 不做，改为「全盘复刻原作构图」
- **决议日**：2026-09-15
- **决议**：不做用户编辑器（连开发期编辑器也不需要）；构图靠**转录原作**而非"设计"；
  设置面板**不提供场景选择**，一切跟随主题
- **遗留**：坐标提取路径 → 见 Q12

### Q6 · 对话框方案 → 沿用选项条设计 + 底部落位
- **决议日**：2026-09-15
- **决议**：对话框 UI 按照选项条（`sel_win`）的设计语言，落位在屏幕底部。
- **产出**：`04-对话框设计规格.md`
- **要点**：
  - 不新造构件，而是把选项条从中部搬到下部并按内容伸缩
  - 高度递推 `H = 86 + 44 × (行数 − 1)`（直接由资源的 86/130 两档推出）
  - 三档色阶（深/中/浅）映射为 agent 状态（空闲 / 生成中 / 等待你）—— **纯状态表达，不引入数值**
- **连带修正**：`03` 的「✗ 底部固定文本框」禁忌改为「✗ 圆角卡片式底部文本框」
- **后续印证**：番外篇剧本中找到 `～選択肢Ｚ～` 与成片的「〜〜、と思った。」选项行，
  文本长度正好落在 86px / 130px 两档上，**与高度递推式吻合**

### Q7 · 完整还原方案 10 项 → 全部决议
- **决议日**：2026-09-15（详见 `09-待确认项决议.md` A1~A10）
- **摘要**：
  - 侧边栏方案**作废**（架构改为无 chrome），改为**整屏書庫 = 木书架 + 书本行**
  - 会话标题**横排**；搜索框移入書庫页，做成「付箋を探す」形态
  - 轨迹用**付箋**；技术细节**默认折叠**
  - 页签条用**细条 `mu_bar`**（仅作降级形态）
  - 底框**兼作输入框**
  - 木框 `archive_frame` **仅元界面用**
  - **不用**图像化字库，改用 Web 明朝体 + **保留袋文字描边**
  - 标题画面 `title_bg1` **用于启动页**

### Q8 · 无界面外壳 7 项 → 全部处理
- **决议日**：2026-09-15
- Q8-1 标签栏 → **尽可能隐藏**，失效时降级为 `mu_bar` 细条
- Q8-2 唤出方式 → 由 Q11 决定（旁边空白区点击 toggle）
- Q8-3 菜单板宽度 → **36.5%**（原作 700/1920），窄屏 `clamp(300px, 36.5%, 46%)`
- Q8-4 轨迹形态 → 归并入 Q7-4（**付箋**）
- Q8-5 音效 → **推迟**（`.hw` = 裸 PCM 可直接转码；已定位 5 个 UI 音效）
- Q8-6 素材分发 → 归并入 Q4（**延后**）
- Q8-7 阅读态输入框 → 归并入 Q7-7（**底框兼作**）

### Q9 · 左键还是右键 → 关闭（由 Q11 取代）
- **决议日**：2026-09-15
- **决议**：不再追问原作。按键语义按用户规格实现（见 Q11）。
- **记录**：源码确证尝试失败的完整原因见 Q11

### Q10 · 交互与自适应 6 项 → 全部处理
- **决议日**：2026-09-15
- Q10-1 左键点菜单项 → **不允许一步到位**，须 Enter 确认
- Q10-2 右键在阅读态 → **唤出菜单**
- Q10-3 底框兼输入框 → 归并入 Q7-7
- Q10-4 极矮窗口 → **不用固定阈值**，改为按底框可用高度**动态降级**（3→2→1 行）
- Q10-5 极致模式 letterbox → **不做**
- Q10-6 `_b_` 系列归属 → 暂定「元界面条目态」，低优先级

### Q11 · 操作逻辑（用户规格）
- **决议日**：2026-09-15
- **决议**：
  - **内容区**（对话框 + 已收到消息）不触发菜单；**旁边空白区**点击 = 唤出菜单；**再点一次** = 收回
  - **光标停在该项上（聚焦态）后按 Enter 才确认**；左键点击本身不确认
- **魔夜同款聚焦效果（逐像素实测）**：
  - `mu_text` 聚焦 = 填充 `#345361` **不变** + **5px 实心 `#58FFFD`** 亮青描边
  - `menu_btn` 聚焦 = 填充 `#B4BAC1` 不变 + `#E2FFFF` 描边；禁用 = 填充压暗为 `#A3A3A3`
  - **统一规则：只加一道发光细边，无位移/缩放/阴影/变色**
- **源码确证尝试（失败，如实记录）**：`.chs` 是字节码，命令表不在脚本；
  `woh_data.dll` 加壳（52MB，22 万条 UTF-16 候选中**零条**含交互词）；
  攻略 Wiki 被 Cloudflare 403；官方页只说「マウス対応」
- **产出**：`08-交互逻辑与自适应方案.md` §1
