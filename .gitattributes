# ============================================================================
# dsh-skin-mahoyo — 行尾策略
# ----------------------------------------------------------------------------
# 本项目的行尾**不是一个口径**，而是有意分开的（实测于 2026-09-23）：
#
#   skin/lib/client.js        CRLF  8224 行
#   skin/lib/index.js         LF     648 行
#   skin/tools/install.ps1    LF     257 行  ← PowerShell 脚本
#   skin/tools/check-*.mjs    CRLF
#   README / MAINTENANCE      CRLF
#
# 历史上因为**行尾不统一**踩过坑（一份"整体 convert"的补丁直接 SKIP，
# 改用逐行替换后又因并行列表长度失配而写回错行尾）。
#
# 所以这里**关掉 git 的全部行尾转换**：仓库里存的就是工作区的原始字节，
# 谁 clone 下来都是同样的 CRLF / LF。不做规范化，也就不会有
# "commit 时转一次、checkout 时又转回来" 的来回抖动，
# 更不会出现 "git status 莫名显示已修改"。
#
# 代价：贡献者若在 Windows 上用会自动补 CRLF 的编辑器改 install.ps1，
# 可能引入 CRLF。**改完请回读行尾构成**（这一条已经写进 MAINTENANCE）。
# ============================================================================

* -text
